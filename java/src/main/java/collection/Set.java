package collection;

/**
 * 
 * @author shani
 *if we don't want duplicates and don't want insertion order then we must go for set Interface
 *set is child interface of collection interface
 *implementation classes for set interface HashSet(LinkedHashSet)
 *
 *
 */
public class Set {

}
